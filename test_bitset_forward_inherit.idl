// Test case for bitset inheritance with forward declarations

bitset Base;  // Forward declaration

// Try to inherit from forward-declared bitset - should fail
bitset Derived : Base {
    uint32 flags;
};

// Definition comes later
bitset Base {
    uint8 status;
};